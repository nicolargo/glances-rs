//! The lazy-with-wake-up collection engine (ARCHITECTURE.md §3, §5):
//! `ensure_plugin` (Idle -> Active transition, first-cycle wait) and
//! `plugin_loop` (collection cycles, idle-timeout shutdown).
//!
//! Implemented in Phase 3 (DEVELOPMENT_PLAN.md).
