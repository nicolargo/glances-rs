//! Configuration: bind address, password, per-plugin refresh/idle settings,
//! CORS allow-list, trusted hosts, `show`/`hide` filters.
//!
//! Implemented in Phase 2 (DEVELOPMENT_PLAN.md), after the discovery order
//! is frozen in Phase 1.
